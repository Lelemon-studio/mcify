{
  "name": "{{name}}",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server built with mcify",
  "scripts": {
    "dev": "mcify dev",
    "build": "mcify build",
    "start": "node dist/server.mjs",
    "generate": "mcify generate"
  },
  "dependencies": {
    "@mcify/core": "^0.0.1-alpha.0",
    "@mcify/runtime": "^0.0.1-alpha.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@mcify/cli": "^0.0.1-alpha.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
