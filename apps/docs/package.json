{
  "name": "mcify-docs",
  "version": "0.0.0",
  "private": true,
  "description": "mcify documentation site (Astro Starlight) — docs.mcify.dev",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "astro build",
    "dev": "astro dev --port 4322",
    "preview": "astro preview --port 4322",
    "og": "node scripts/generate-og.mjs",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(lint runs at root)\"",
    "typecheck": "astro check --tsconfig tsconfig.json",
    "clean": "rimraf dist .astro .turbo"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.32.0",
    "astro": "^5.0.5",
    "sharp": "^0.33.5",
    "starlight-llms-txt": "^0.6.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@resvg/resvg-js": "^2.6.2",
    "wrangler": "^3.90.0"
  }
}
