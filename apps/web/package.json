{
  "name": "mcify-web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "mcify.dev — landing site",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "astro dev --port 4399",
    "build": "astro build",
    "preview": "astro preview --port 4399",
    "test": "vitest run --passWithNoTests",
    "lint": "echo \"(lint runs at root)\"",
    "typecheck": "astro check --tsconfig tsconfig.json",
    "clean": "rimraf dist .astro .turbo"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.1",
    "@astrojs/tailwind": "^5.1.4",
    "astro": "^5.0.5",
    "tailwindcss": "^3.4.17",
    "wrangler": "^3.90.0"
  }
}
